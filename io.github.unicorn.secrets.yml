app-id: io.github.unicorn.secrets
runtime: org.gnome.Platform
runtime-version: '42'
sdk: org.gnome.Sdk
command: run.sh
modules:
  - name: secrets
    buildsystem: simple
    build-commands:
      - install -D run.sh /app/bin/run.sh
      - install -D secrets.py /app/bin/secrets.py
      - install -D io.github.unicorn.secrets.desktop /app/share/applications/io.github.unicorn.secrets.desktop
      - install -D io.github.unicorn.secrets.appdata.xml /app/share/metainfo/io.github.unicorn.secrets.appdata.xml
      - install -D io.github.unicorn.secrets.svg /app/share/icons/io.github.unicorn.secrets.svg
      - install -D secrets.ui /app/bin/secrets.ui
    sources:
      - type: file
        path: secrets.py
      - type: file
        path: run.sh
      - type: file
        path: io.github.unicorn.secrets.desktop
      - type: file
        path: io.github.unicorn.secrets.appdata.xml
      - type: file
        path: io.github.unicorn.secrets.svg
      - type: file
        path: secrets.ui

finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=fallback-x11
  # Wayland access
  - --socket=wayland